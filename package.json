{
  "name": "nx-monorepo",
  "private": true,
  "devDependencies": {
    "@changesets/cli": "^2.0.0",
    "@jscutlery/semver": "^5.6.0",
    "@nx/js": "^18.0.0",
    "nx": "^18.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "show:affected": "nx show projects --affected --base=origin/main",
    "gen:changeset": "nx affected --target=version --skip-git --parallel=1",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "publish-packages": "changeset publish"
  }
}
