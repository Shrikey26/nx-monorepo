{
  "name": "nx-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@changesets/cli": "^2.0.0",
    "@nx/js": "^18.0.0",
    "nx": "^18.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "show:affected": "pnpm nx show projects --affected --files",
    "gen:changeset": "node scripts/gen-changeset.js",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "publish-packages": "changeset publish"
  }
}
